name: slide_digital_clock
description: Digital Clock for Flutter
version: 1.0.3
homepage: https://github.com/caglarylmz/slide_digital_clock

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
    
flutter:
  uses-material-design: true
